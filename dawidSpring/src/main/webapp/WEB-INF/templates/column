

<div th:fragment="copy">
	<footer>
        <p>© The Tasks 2017</p>
    </footer>
</div>

<div th:fragment="column(col)">
	<h4 th:text="${col.title}" />
	<table th:if="${not #lists.isEmpty(col.tasks)}" border="1">
	     <tr>
	        <th>name</th>
	        <th>description</th>
	        <th>labels</th>
	        <th>edit</th>
	    </tr>
	    <tr th:each="task : ${#lists.sort(col.tasks)}">
	        <td>
	            <span th:text="${task.name}" />

	        </td>
	         <td th:text="${task.desc}"/>
	        <td>
	            <span th:each="label: ${#lists.sort(task.labels)}" th:text="${label.description}"
                	                th:style="'color:'+${label.colour}" />
	        </td>
            <td>
                <p class="text-center">
                    <a href="#" class="btn btn-primary btn-lg" role="button" data-toggle="modal" th:attr="data-target='#login-modal-'+${task.id }">edit</a>
                </p>





                <div class="modal fade" th:id="'login-modal-' + ${task.id}" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true" style="display: none;">
                   <div class="modal-dialog">
                   			<div class="modal-content">
                   				<div class="modal-header" align="center">
                   					<img class="img-circle" id="img_logo" src="http://bootsnipp.com/img/logo.jpg">
                   					<button type="button" class="close" data-dismiss="modal" aria-label="Close">
                   						<span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
                   					</button>
                   				</div>

                                   <!-- Begin # DIV Form -->
                                   <span th:text="${task.name}" />
                                   <div id="div-forms">
                                        <form action="#" th:action="@{/editTask}" th:object="${newTask}" method="post">
                                            <input type=hidden th:name="userNick" th:value="${user.nickname}"/>
                                            <table border="1">
                                                <tr>
                                                    <td><label th:text="name" /></td>
                                                    <td><input type="text" th:field="*{name}" th:value="${task.name}" /></td>
                                                </tr>
                                                <tr>
                                                    <td><label th:text="description" /></td>
                                                    <td><input type="text" th:field="*{desc}" /></td>
                                                </tr>
                                                <tr>
                                                    <td><input type="submit" value="Submit"/></td>
                                                </tr>
                                            </table>
                                        </form>
                                   </div>

                   			</div>
                   		</div>
                </div>
            </td>

	    </tr>
	</table>
</div>
