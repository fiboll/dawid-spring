<!DOCTYPE html SYSTEM "http://www.thymeleaf.org/dtd/xhtml1-strict-thymeleaf-spring3-3.dtd">
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Contacts</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
</head>
<body>


<div th:if="${not #lists.isEmpty(users)}">
	<div th:each="user: ${users}">
		<span th:text="${user.firstName + ' ' + user.secondName}"></span>
		<table th:if="${not #lists.isEmpty(user.tasks)}" border="1">
			 <tr>
        		<th>name</th>
        		<th>description</th>
    		</tr>	
			<tr th:each="task : ${user.tasks}">
				<td>
					<span th:text="${task.name}" />
					<span th:each="label: ${task.labels}" th:text="${label.description}" 
						th:style="'color:'+${label.colour}" />
				</td>
				<td th:text="${task.desc}"/>
			</tr>
		</table>
	</div>
</div>
	

</body>
</html>